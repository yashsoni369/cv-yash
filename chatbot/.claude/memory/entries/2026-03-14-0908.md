<!-- session: unknown | trigger: auto -->
## Session Memory — 2026-03-14 09:08

**What happened**: Fixed voice mode audio capture by adding explicit `AudioContext.resume()` after user gesture, protecting the timer against audio capture errors, removing audio feedback loop, and fixing stale closure in WebSocket close handler. User reports audio is now being captured but OpenAI isn't responding—added detailed logging to diagnose the issue via browser console.

**Key decisions**:
- Decided to call `AudioContext.resume()` explicitly because user gesture expires after multiple awaits, leaving context in "suspended" state where `onaudioprocess` never fires.
- Decided to use silent `GainNode` (gain=0) instead of removing the processor connection because we need `onaudioprocess` to trigger for audio capture without playing mic through speakers.
- Decided to add console logging for all Realtime API events to debug why OpenAI doesn't respond after audio is captured.

**Artifacts**:
- `/Users/santifer/code/project2/cv-santiago/src/useVoiceMode.ts` — Multiple edits: added `audioContext.resume()`, wrapped timer in try-catch, added silent gain node, fixed ws.onclose closure, added detailed event logging.

**Open threads**:
- Voice mode captures audio but orb spins indefinitely without OpenAI response. Need to check console logs to see if `input_audio_buffer.speech_stopped`, `response.created`, or error events arrive from OpenAI API.
- Evals passing at 98% (54/55) but real-time voice interaction still broken.

**Context for next session**: The audio pipeline is now working up to capture, but the OpenAI Realtime API isn't responding. Check the console logs in the browser to see which events are being received from OpenAI—this will reveal whether the issue is VAD detection, API errors, or connection problems. User is on mobile via tmux so can't easily debug locally; use Playwright to simulate full voice flow and check responses.
